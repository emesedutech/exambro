plugins {
    id 'com.android.application'
}

android {
    compileSdk 34
    namespace "id.emes.exambrowser"

    defaultConfig {
        applicationId "id.emes.exambrowser"
        minSdk 21
        targetSdk 34
        versionCode 2
        versionName "1.1.0"
    }

    signingConfigs {
        release {
            def ksFile = file("keystore.jks")
            if (ksFile.exists()) {
                storeFile     ksFile
                storePassword System.getenv("STORE_PASSWORD") ?: ""
                keyAlias      System.getenv("KEY_ALIAS")      ?: ""
                keyPassword   System.getenv("KEY_PASSWORD")   ?: ""
            }
        }
    }

    buildTypes {
        release {
            // ✅ WAJIB false — ini penyebab masalah APK bertanda debuggable
            debuggable      false
            minifyEnabled   true
            shrinkResources true
            crunchPngs      false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            // Jika keystore.jks ada → pakai release key, jika tidak → debug key otomatis
            if (file("keystore.jks").exists()) {
                signingConfig signingConfigs.release
            }
        }
        debug {
            debuggable true
            crunchPngs false
        }
    }

    // Split APK per arsitektur — lebih kecil untuk distribusi langsung ke sekolah
    splits {
        abi {
            enable       true
            reset()
            include      "armeabi-v7a", "arm64-v8a"
            universalApk true   // APK universal sebagai fallback jika tidak tahu tipe HP
        }
    }

    aaptOptions {
        cruncherEnabled false
        noCompress 'png'
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'com.google.zxing:core:3.5.3'
}
